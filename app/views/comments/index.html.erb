<p style="color: green"><%= notice %></p>

<h1>Comentarios</h1>

<div id="comments">
  <% @comments.each do |comment| %>
    <%= render comment %>
    <p>
      <%= link_to "Ver Comentario", comment %>
    </p>
  <% end %>
</div>

<%= link_to "Crea un Comentario", new_comment_path %>
